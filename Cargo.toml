[package]
name = "fluid-sim"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "*", features = ["dynamic_linking", "experimental_bevy_ui_widgets"] }
rand = "0.10.0"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
